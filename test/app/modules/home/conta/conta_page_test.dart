import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('ContaPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ContaPage(title: 'Conta')));
    //  final titleFinder = find.text('Conta');
    //  expect(titleFinder, findsOneWidget);
  });
}
