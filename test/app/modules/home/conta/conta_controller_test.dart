import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:controlmoney/app/modules/home/home_module.dart';

void main() {
  initModule(HomeModule());
  // ContaController conta;
  //
  setUp(() {
    //     conta = HomeModule.to.get<ContaController>();
  });

  group('ContaController Test', () {
    //   test("First Test", () {
    //     expect(conta, isInstanceOf<ContaController>());
    //   });

    //   test("Set Value", () {
    //     expect(conta.value, equals(0));
    //     conta.increment();
    //     expect(conta.value, equals(1));
    //   });
  });
}
